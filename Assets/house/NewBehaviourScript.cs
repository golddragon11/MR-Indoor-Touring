using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;
using System;
public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    /*public class NodeProperty
    {
        public class node
        {
            public int x, z;
            public List<Tuple<int, float>> AdList { get; set; }
        }
        public node(int x, int z, List<Tuple<int, float>> AdList)
        {
            this.x = x;
            this.z = z;
            this.AdList = AdList.ToList();
            //List2 = List1.ToList();

        }
    }
    public List<node> floor_node { get; set; }
    floor_node.add()     */
}
